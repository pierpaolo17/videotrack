define([],function(){var CAPABILITIES={youtube:{currentTime:['getCurrentTime'],duration:['getDuration'],play:['playVideo'],pause:['pauseVideo'],seek:['seekTo'],playbackRate:['getPlaybackRate','setPlaybackRate'],readPlaybackRate:['getPlaybackRate'],writePlaybackRate:['setPlaybackRate'],ended:['getPlayerState']},html5:{currentTime:[],duration:[],play:['play'],pause:['pause'],seek:[],playbackRate:[],readPlaybackRate:[],writePlaybackRate:[],volume:[],mute:[],paused:[],ended:[]},vimeo:{currentTime:['getCurrentTime'],duration:['getDuration'],play:['play'],pause:['pause'],seek:['setCurrentTime'],playbackRate:['getPlaybackRate','setPlaybackRate'],readPlaybackRate:['getPlaybackRate'],writePlaybackRate:['setPlaybackRate']}};function normaliseProviderType(providerType){var type=String(providerType||'').toLowerCase();return Object.prototype.hasOwnProperty.call(CAPABILITIES,type)?type:'';}function isKnownProviderType(providerType){return normaliseProviderType(providerType)!=='';}function getCapabilityMethods(providerType,capability){var providerCapabilities=CAPABILITIES[String(providerType||'').toLowerCase()];if(!providerCapabilities||!Object.prototype.hasOwnProperty.call(providerCapabilities,capability)){return[];}return providerCapabilities[capability].slice(0);}function can(provider,providerType,capability){var type=normaliseProviderType(providerType);if(!type){return false;}return isAvailable(provider,getCapabilityMethods(type,capability));}function hasCapability(provider,providerType,capability,fallbackMethods){if(capability){return can(provider,providerType,capability);}if(fallbackMethods&&fallbackMethods.length){return isAvailable(provider,fallbackMethods);}var type=normaliseProviderType(providerType);if(!type){return false;}return isAvailable(provider);}function getCapabilities(providerType){var providerCapabilities=CAPABILITIES[String(providerType||'').toLowerCase()]||{};var copy={};Object.keys(providerCapabilities).forEach(function(key){copy[key]=providerCapabilities[key].slice(0);});return copy;}function canCurrentTime(provider,providerType){return can(provider,providerType,'currentTime');}function canDuration(provider,providerType){return can(provider,providerType,'duration');}function canPlay(provider,providerType){return can(provider,providerType,'play');}function canPause(provider,providerType){return can(provider,providerType,'pause');}function canSeek(provider,providerType){return can(provider,providerType,'seek');}function canPlaybackRate(provider,providerType){return can(provider,providerType,'playbackRate');}function canVolume(provider,providerType){return can(provider,providerType,'volume');}function canMute(provider,providerType){return can(provider,providerType,'mute');}function canPaused(provider,providerType){return can(provider,providerType,'paused');}function canEnded(provider,providerType){return can(provider,providerType,'ended');}function normaliseTime(value,fallback){var time=Number(value);if(isFinite(time)&&time>=0){return time;}var fallbackTime=Number(fallback);return isFinite(fallbackTime)&&fallbackTime>=0?fallbackTime:0;}function resolveSkipTarget(current,delta,duration){var target=normaliseTime(current,0)+Number(delta||0);if(!isFinite(target)){target=normaliseTime(current,0);}target=Math.max(0,target);var safeDuration=Number(duration);if(isFinite(safeDuration)&&safeDuration>0){target=Math.min(safeDuration,target);}return target;}function getCurrentTime(state,getter,log,label){var fallback=state&&state.lasttime;try{if(typeof getter==='function'){var current=normaliseTime(getter(),fallback);if(state){state.lasttime=current;}return current;}}catch(error){if(log&&typeof log.debug==='function'){log.debug('mod_videotrack:could not read '+(label||'player')+' current time-'+error);}}return normaliseTime(fallback,0);}function getDuration(state,getter,log,label){var fallback=state&&state.duration;try{if(typeof getter==='function'){var duration=normaliseTime(getter(),fallback);if(state&&duration>0){state.duration=duration;}return duration;}}catch(error){if(log&&typeof log.debug==='function'){log.debug('mod_videotrack:could not read '+(label||'player')+' duration-'+error);}}return normaliseTime(fallback,0);}function isAvailable(provider,methods){if(!provider){return false;}if(!methods||!methods.length){return true;}return methods.every(function(method){return typeof provider[method]==='function';});}function normaliseVolume(value,fallback){var volume=Number(value);if(isFinite(volume)){return Math.max(0,Math.min(1,volume));}var fallbackVolume=Number(fallback);return isFinite(fallbackVolume)?Math.max(0,Math.min(1,fallbackVolume)):1;}function getVolume(state,getter,log,label){var fallback=state&&state.volume;try{if(typeof getter==='function'){var volume=normaliseVolume(getter(),fallback);if(state){state.volume=volume;}return volume;}}catch(error){if(log&&typeof log.debug==='function'){log.debug('mod_videotrack:could not read '+(label||'player')+' volume-'+error);}}return normaliseVolume(fallback,1);}function setVolume(volume,setter,state,log,label){var safeVolume=normaliseVolume(volume,state&&state.volume);return run(function(){var result=setter(safeVolume);if(state){state.volume=safeVolume;state.muted=(safeVolume===0);}return result;},log,label||'set volume');}function isMuted(state,getter,log,label){var fallback=state&&typeof state.muted==='boolean'?state.muted:false;try{if(typeof getter==='function'){var muted=!!getter();if(state){state.muted=muted;}return muted;}}catch(error){if(log&&typeof log.debug==='function'){log.debug('mod_videotrack:could not read '+(label||'player')+' muted state-'+error);}}return fallback;}function setMuted(muted,setter,state,log,label){var safeMuted=!!muted;return run(function(){var result=setter(safeMuted);if(state){state.muted=safeMuted;}return result;},log,label||'set muted');}function getPlaybackRate(state,getter,log,label){var fallback=state&&state.playbackrate;try{if(typeof getter==='function'){var rate=Number(getter());if(isFinite(rate)&&rate>0){if(state){state.playbackrate=rate;}return rate;}}}catch(error){if(log&&typeof log.debug==='function'){log.debug('mod_videotrack:could not read '+(label||'player')+' playback rate-'+error);}}var fallbackRate=Number(fallback);return isFinite(fallbackRate)&&fallbackRate>0?fallbackRate:1;}function setPlaybackRate(rate,setter,state,log,label){var safeRate=Number(rate);if(!isFinite(safeRate)||safeRate<=0){safeRate=1;}return run(function(){var result=setter(safeRate);if(state){state.playbackrate=safeRate;}return result;},log,label||'set playback rate');}function isPaused(state,getter,log,label){var fallback=state&&typeof state.playing==='boolean'?!state.playing:true;try{if(typeof getter==='function'){return!!getter();}}catch(error){if(log&&typeof log.debug==='function'){log.debug('mod_videotrack:could not read '+(label||'player')+' paused state-'+error);}}return fallback;}function isEnded(state,getter,log,label){var fallback=state&&typeof state.ended==='boolean'?state.ended:false;try{if(typeof getter==='function'){var ended=!!getter();if(state){state.ended=ended;}return ended;}}catch(error){if(log&&typeof log.debug==='function'){log.debug('mod_videotrack:could not read '+(label||'player')+' ended state-'+error);}}return fallback;}function run(action,log,label){try{if(typeof action==='function'){return action();}}catch(error){if(log&&typeof log.debug==='function'){log.debug('mod_videotrack:player adapter command failed'+(label?'('+label+')':'')+'-'+error);}}return null;}function play(action,log,label){return run(action,log,label||'play');}function pause(action,log,label){return run(action,log,label||'pause');}function seek(target,seeker,log,label){var safeTarget=normaliseTime(target,0);return run(function(){return seeker(safeTarget);},log,label||'seek');}return{getCapabilities:getCapabilities,getCapabilityMethods:getCapabilityMethods,normaliseProviderType:normaliseProviderType,isKnownProviderType:isKnownProviderType,can:can,hasCapability:hasCapability,canCurrentTime:canCurrentTime,canDuration:canDuration,canPlay:canPlay,canPause:canPause,canSeek:canSeek,canPlaybackRate:canPlaybackRate,canVolume:canVolume,canMute:canMute,canPaused:canPaused,canEnded:canEnded,normaliseTime:normaliseTime,normaliseVolume:normaliseVolume,resolveSkipTarget:resolveSkipTarget,isAvailable:isAvailable,getCurrentTime:getCurrentTime,getDuration:getDuration,getVolume:getVolume,setVolume:setVolume,isMuted:isMuted,setMuted:setMuted,getPlaybackRate:getPlaybackRate,setPlaybackRate:setPlaybackRate,isPaused:isPaused,isEnded:isEnded,run:run,play:play,pause:pause,seek:seek};});
