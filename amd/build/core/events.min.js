define([], function () { function create() { var handlers = {}; return { on: function (name, handler) { if (typeof name !== 'string' || typeof handler !== 'function') { return function () { }; } handlers[name] = handlers[name] || []; handlers[name].push(handler); return function () { handlers[name] = (handlers[name] || []).filter(function (candidate) { return candidate !== handler; }); }; }, off: function (name, handler) { if (!handlers[name]) { return; } handlers[name] = handlers[name].filter(function (candidate) { return candidate !== handler; }); }, emit: function (name, payload) { var eventPayload = payload || {}; return (handlers[name] || []).slice().map(function (handler) { try { return handler(eventPayload); } catch (error) { if (typeof window !== 'undefined' && window.console && window.console.warn) { window.console.warn('mod_videotrack event handler error', error); } return null; } }); }, once: function (name, handler) { var unsubscribe = function () { }; var wrapped = function (payload) { unsubscribe(); return handler(payload || {}); }; unsubscribe = this.on(name, wrapped); return unsubscribe; }, count: function (name) { if (typeof name === 'string') { return (handlers[name] || []).length; } return Object.keys(handlers).reduce(function (total, key) { return total + handlers[key].length; }, 0); }, clear: function () { handlers = {}; } }; } function ensure(state) { if (!state) { return create(); } if (!state.events || typeof state.events.emit !== 'function') { state.events = create(); } return state.events; } function emit(state, name, payload) { if (!state || !state.events || typeof state.events.emit !== 'function') { return []; } return state.events.emit(name, payload || {}); } return { create: create, ensure: ensure, emit: emit }; });
